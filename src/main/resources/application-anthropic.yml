spring:
  ai:
    anthropic:
      api-key: ${ANTHROPIC_API_KEY}  # 从环境变量读取
      base-url: https://api.anthropic.com
      chat:
        options:
          model: claude-3-5-sonnet-20241022
          temperature: 0.7
          max-tokens: 1000
